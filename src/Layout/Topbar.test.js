import { mount } from "@vue/test-utils";
import Topbar from "./Topbar.vue";
import { describe, expect, test,it } from "vitest";

describe("Topbar.vue", () => {
    
    test("Topbar the correct style for error", () => {
        const wrapper = mount(Topbar)
    });
   
});
